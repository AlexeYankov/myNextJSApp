

const Home = () => {
  return (
    // <div id={'sign-in'} className={s.container}>
    <div id={'sign-in'} className={''}>
      {/* <ServiceAuth page={'SignInPage'} />
      <SignInForm /> */}
    </div>
  );
};

export default Home;